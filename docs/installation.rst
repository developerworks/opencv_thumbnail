.. highlight:: shell

============
Installation
============

At the command line:

.. code-block:: console

    $ easy_install opencv_thumbnail

Or, if you have virtualenvwrapper installed:

.. code-block:: console

    $ mkvirtualenv opencv_thumbnail
    $ pip install opencv_thumbnail
